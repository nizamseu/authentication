import React from 'react';
export const authContrast =React.createContext();

const ContrastApi = () => {

    return (
        <div>
            
        </div>
    );
};

export default ContrastApi;